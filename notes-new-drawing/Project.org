# Objects need to be initialized
## When opening note or notebook
1. EditorView
2. surfaceview created
3. createTouchHelper
4. viewModel (which should be a EditorViewModel)
   
need to change so that createtouch helper is called in
correct order

Problem: viewportManager is not created in OnyxDrawingActivity
Happens because ViewModel is null

ViewModel created in EditorView and is set as viewmodel for
OnyxDrawingActivity in EditorView constructor

Where is OnyxStylushandler created?
  In createTouchHelper
Where is createTouchHelper called?
  In handleSurfaceViewCreated
Where is handleSurfaceViewCreated called?
  When surfaceview is created in DrawingCanvas
Where is DrawingCanvas created?
  When EditorView is created
Where is EditorView created?
  When OnyxDrawingActivity is created (onCreate fun)

  OnyxDrawingactivity -> EditorView -> DrawingCanvas -> SurfaceView
  SurfaceView created -> OnyxStylushandler created  

# Order of initialization when opening note/notebook
1. OnyxDrawingactivity created
   1. Database
   2. Repositories (note, notebook, folder)
   3. Paint
   4. DeciveReciever
   5. EditorView
      Initializes
      Calls
        1. handleSurfaceViewCreated
            Initializes
            Calls
              1. createTouchHelper
                 Initializes
                  - OnyxStylusHandler (touchHelper)
                  - gestureHandler
                 Calls
               

